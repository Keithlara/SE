Fix and debug my Hotel Reservation and Billing System (PHP, MySQL, Bootstrap 5) with the following issues:

⚠️ STRICT RULES:

Do NOT break existing features
Do NOT remove working modules
Only fix bugs and improve logic
Use prepared statements
Maintain current system structure
🔴 ISSUE 1: GUEST LOGIN NOT WORKING
PROBLEM:

Guest accounts cannot login / system cannot read account

FIX:
Ensure login supports BOTH:
username
email
SELECT * FROM users WHERE username = ? OR email = ?
Fix password verification:
if (password_verify($password, $user['password'])) {
    // login success
}
Store session properly:
$_SESSION['user_id'] = $user['id'];
$_SESSION['role'] = $user['role'];
$_SESSION['is_verified'] = $user['is_verified'];
Debug:
Check if user exists in DB
Check if password is hashed
Check if role = guest is allowed
🔴 ISSUE 2: FORGOT PASSWORD → SERVER SHUTDOWN ERROR
PROBLEM:

Email sending crashes system

FIX USING PHPMailer:
Install PHPMailer (if not yet)
Replace mail() with PHPMailer SMTP:
use PHPMailer\PHPMailer\PHPMailer;

$mail = new PHPMailer(true);

$mail->isSMTP();
$mail->Host = 'smtp.gmail.com';
$mail->SMTPAuth = true;
$mail->Username = 'your_email@gmail.com';
$mail->Password = 'your_app_password';
$mail->SMTPSecure = 'tls';
$mail->Port = 587;

$mail->setFrom('your_email@gmail.com', 'Travelers Place');
$mail->addAddress($user_email);

$mail->Subject = 'Reset Password';
$mail->Body = 'Click this link to reset: ' . $reset_link;

$mail->send();
FIX COMMON ERRORS:
Use Gmail App Password (NOT normal password)
Enable “Less secure app” OR App Password
Wrap in try-catch:
try {
    $mail->send();
} catch (Exception $e) {
    echo $mail->ErrorInfo;
}
🔴 ISSUE 3: EMAIL VERIFICATION NOT WORKING
REQUIREMENT:
User can register
User can login
BUT cannot book until verified
DATABASE FIX:
ALTER TABLE users ADD COLUMN is_verified TINYINT(1) DEFAULT 0;
ALTER TABLE users ADD COLUMN verification_code VARCHAR(255);
REGISTRATION FIX:
$code = bin2hex(random_bytes(16));

INSERT INTO users (..., is_verified, verification_code)
VALUES (..., 0, '$code');
SEND VERIFICATION EMAIL:
$verify_link = "http://localhost/project/verify.php?code=" . $code;
CREATE verify.php:
$code = $_GET['code'];

$stmt = $conn->prepare("SELECT id FROM users WHERE verification_code=?");
$stmt->bind_param("s", $code);
$stmt->execute();
$result = $stmt->get_result();

if ($result->num_rows > 0) {
    $stmt = $conn->prepare("UPDATE users SET is_verified=1 WHERE verification_code=?");
    $stmt->bind_param("s", $code);
    $stmt->execute();

    echo "Account verified successfully!";
} else {
    echo "Invalid verification link";
}
🔴 ISSUE 4: BLOCK BOOKING IF NOT VERIFIED
ADD THIS CHECK IN BOOKING MODULE:
if ($_SESSION['is_verified'] == 0) {
    echo "<script>alert('Please verify your email before booking.'); window.location='profile.php';</script>";
    exit;
}
🔴 ISSUE 5: ADD WARNING UI
<div class="alert alert-warning text-center">
Please verify your email to unlock booking features.
</div>
🔴 ISSUE 6: RESEND VERIFICATION EMAIL

Add button in profile:

<button id="resend">Resend Verification Email</button>

Backend:

reuse existing verification_code
resend email
✅ EXPECTED RESULT

✔ Guest login works (username/email)
✔ Password is verified correctly
✔ Forgot password sends email without crash
✔ Email verification works
✔ Users can login even if not verified
✔ Users CANNOT book until verified
✔ Warning message is shown
✔ No backend errors

🔥 BONUS (OPTIONAL)
Auto login after verification
Expire verification link after use
Show “Verified ✅” badge in profile